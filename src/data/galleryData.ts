export type GalleryItem = {
    id: string;
    title: string;
    category: string;
    client: string;
    imageUrl: string;
    description: string;
    metrics: string;
};

export const galleryData: GalleryItem[] = [];
